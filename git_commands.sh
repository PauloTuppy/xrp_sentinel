# Create and push to GitHub repository using GitHub CLI
gh repo create PauloTuppy/xrp_sentinel --public --source=. --push

# Push with a descriptive message for visibility
git add .
git commit -m "Latest updates - push for show"
git push
